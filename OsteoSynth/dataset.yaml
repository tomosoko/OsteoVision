path: ~/Documents/DRR_Factory/yolo_dataset # absolute dataset path
train: images/train # train images (relative to 'path')
val: images/val # val images — MUST be separate from train for overfitting detection

# Classes
names:
  0: knee_joint

# Keypoints
kpt_shape: [4, 3] # [number of keypoints, number of dimensions (x, y, visible)]
flip_idx: [0, 2, 1, 3] # Indices for left-right flip augmentation (medial/lateral swap)
